namespace Common
{
    public static class Constants
    {
        public const string ConnectionString = @"server=(localdb)\MSSQLLocalDB; initial catalog = rebus; integrated security = true";
        // public const string ConnectionString = @"server=.; initial catalog = rebus; integrated security = true";
    }
}
